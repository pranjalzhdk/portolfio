# Copy to .env.local and fill in your token.
# On Vercel: Project Settings → Environment Variables → add all three, then redeploy.

NEXT_PUBLIC_SANITY_PROJECT_ID=ry91b1jx
NEXT_PUBLIC_SANITY_DATASET=production

# Create at sanity.io/manage → API → Tokens (Editor permissions)
SANITY_API_TOKEN=
